package ScreenCaptureSoft.Test;

import javax.swing.*;

public class SwingJFrameTest extends JFrame {
    public SwingJFrameTest() {
        setSize(80, 500);
        setLocation(20, 20);
        setUndecorated(true);
        setVisible(true);
    }
}
